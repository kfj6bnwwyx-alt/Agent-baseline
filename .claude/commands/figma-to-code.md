Run the figma-to-code chain for $ARGUMENTS.

Follow `orchestration/chains/figma-to-code.chain.md`:
1. Use metadata-extractor to pull from Figma via Console MCP
2. Use context-engine agent to generate/update Layer 3 files
3. Use code-generator to implement the component

Component: $ARGUMENTS
