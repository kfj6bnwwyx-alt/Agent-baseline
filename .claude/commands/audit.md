Run a design system audit on $ARGUMENTS.

Use the design-system-auditor agent.

Load the relevant contract from `knowledge-base/design-system/contracts/` and audit the implementation against all 7 dimensions.

If $ARGUMENTS is "system" or "all", run a full system health audit per `orchestration/chains/system-health.chain.md`.

Target: $ARGUMENTS
