# Code Standards

Replace with your team's standards.

## General
- TypeScript strict mode
- Named exports over default exports
- One component per file
- Co-locate tests with source

## Component standards
- All components consume design tokens (never hardcode values)
- Every component has TypeScript types for props
- Include error and loading states
- Accessible by default (keyboard, screen reader, focus management)
